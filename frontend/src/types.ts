// types.ts

export interface TaskTypeOption {
    id: string;
    label: string;
    description: string;
}
  
export interface CustomInstructionOption {
    id: string;
    label: string;
    description: string;
}
  